A robust test automation framework built with Python, utilizing Behave for BDD, Playwright for browser automation, and BehaveX for parallel execution. The framework supports Allure and BehaveX HTML reports.

Setup:
1. Execute `pip3 install -r requirements.txt`
2. Execute `playwright install` (To install Playwright browsers)

Start Tests:
- Execute `python3 runner.py`
